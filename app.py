import streamlit as st
import pandas as pd

# إعدادات الصفحة
st.set_page_config(page_title="نظام حجز المختبر", layout="wide")

st.title("📅 نظام حجز المختبر (النسخة الاحتياطية)")

# إنشاء جدول افتراضي فارغ في حال عدم وجود ربط
days = ['الأحد', 'الاثنين', 'الثلاثاء', 'الأربعاء', 'الخميس']
periods = ['الحصة 1', 'الحصة 2', 'الحصة 3', 'الحصة 4', 'الحصة 5', 'الحصة 6', 'الحصة 7']

df = pd.DataFrame('', index=periods, columns=days)

st.write("### جدول الحجوزات الحالي:")
st.table(df)

st.info("ملاحظة: هذا الجدول يعمل الآن بشكل مستقل (بدون ربط سحابي) لتجنب الأخطاء.")
